<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
  </PropertyGroup>

  <PropertyGroup>
      <PackageLicenseFile>LICENSE.md</PackageLicenseFile>
      <Description>POCO stands for "Plain Old C# Object" or "Plain Old CLR Object", depending on who you ask. This library is a custom mapper for POCOs (map values of identical properties from one POCO to another POCO).</Description>
      <Copyright>Nor Gelera 2019</Copyright>
      <PackageProjectUrl>https://github.com/norgelera/poco.mapper</PackageProjectUrl>
      <RepositoryUrl>https://github.com/norgelera/poco.mapper</RepositoryUrl>
      <PackageTags>poco mapper entity class equals</PackageTags>
      <RepositoryType>Git</RepositoryType>
      <Authors>Nor Gelera</Authors>
      <Product>POCO.Mapper</Product>
      <Version>1.2.0</Version>
      <PackageReleaseNotes>- Fixed an issue with mapping values in IList&lt;&gt; and List&lt;&gt;
- Fixed an issue when a collection to be mapped is empty
- Added support for Array[] properties
- IList, List and Array[] properties can be mapped to each other
- When using IDictionary or other collection types, expect to get an exception
- Improved deep mapping of POCO for list and arrays</PackageReleaseNotes>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <DocumentationFile>E:\DEV\POCOMapper\POCOMapper\POCOMapper.xml</DocumentationFile>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'">
    <DocumentationFile>E:\DEV\POCOMapper\POCOMapper\POCOMapper.xml</DocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <None Include="LICENSE.md" Pack="true" PackagePath="" />
  </ItemGroup>

</Project>
